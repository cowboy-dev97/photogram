<nav class="bg-gray-800 h-12 mx-auto flex items-center justify-between text-gray-400">
  <p><%= link_to "Photogram", root_path, class: "font-bold text-gray-300 hover:text-gray-50 h-full p-5" %></p>
  <% if user_signed_in? %>
    <p class="text-white bg-indigo-700 rounded-lg px-4 py-1">Logged in as: <%= current_user.user_name %></p>
  <% end%>
  <ul class="flex h-full list-none items-center gap-4 pr-5">
    <% if user_signed_in? %>
      <li><%= link_to "New post", new_post_path, class:"hover:text-gray-50 h-full" %></li>
      <li><%= button_to "Log out", destroy_user_session_path, method: :delete,  data: {turbo_confirm: "Are you sure you want to log out?"}, class:"text-red-500 hover:text-gray-50 h-full cursor-pointer" %></li>
    <% else %>
      <li><%= link_to "Log in", new_user_session_path, class:"hover:text-gray-50 h-full" %></li>
      <li><%= link_to "Register", new_user_registration_path, class:"text-indigo-500 hover:text-gray-50 h-full" %></li>
    <% end%>
  </ul>
</nav>